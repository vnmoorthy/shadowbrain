// Gold corpus + 50 hand-labeled queries for the retriever benchmark.
//
// The corpus is 60 entries across 5 representative repos. The full 1,000-
// entry corpus mentioned in the spec is generated by replicating + perturbing
// these 60 (see test/fixtures/gen-corpus.mjs) — the labeled queries point
// at the seed entries so we can grow the corpus without re-labeling.

export const REPOS = [
  'github.com/acme/api',
  'github.com/acme/web',
  'github.com/acme/infra',
  'github.com/lab/research',
  'github.com/zoo/oss',
];

let _id = 0;
const next = () => `seed-${++_id}`;

export const SEED_ENTRIES = [
  // ── github.com/acme/api ────────────────────────────────────────────
  { id: next(), repo: 'github.com/acme/api', topic: 'auth', kind: 'pattern',
    title: 'Use jose for JWT verification',
    body: 'We standardized on the jose library for JWT verification. It supports all signing algorithms used by our IdPs (RS256, ES256, EdDSA) and exposes a clean API. Replace any usage of jsonwebtoken with jose.',
    tags: ['jwt', 'auth', 'security'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'auth', kind: 'gotcha',
    title: 'Auth0 rotates JWKs every 24h — cache must respect maxAge',
    body: 'Auth0 rotates JWKs roughly every 24 hours. Our jose verifier was caching JWKs for 7 days, which produced intermittent 401s after rotations. Set jwks.maxAge to 6h.',
    tags: ['jwt', 'auth0', 'jwks'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'billing', kind: 'pattern',
    title: 'Stripe webhook idempotency',
    body: 'All Stripe webhooks must be processed idempotently. Use the stripe-signature header for verification and the event.id for dedup. Store processed event ids in Redis for 24h.',
    tags: ['stripe', 'webhook', 'idempotency'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'billing', kind: 'gotcha',
    title: 'Stripe idempotency_key only valid for 24h, retry beyond that fails',
    body: 'Stripe idempotency keys expire after 24 hours. Retries beyond that window get a fresh charge. Our retry queue had no TTL, so a 36h-stale message double-charged a customer.',
    tags: ['stripe', 'idempotency', 'incident'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'database', kind: 'decision',
    title: 'Postgres over MySQL — chose PG 16',
    body: 'Decided in Q1 to standardize on Postgres 16 across all services. Reasons: pgvector for embeddings, partitioning maturity, JSON ergonomics. Existing MySQL services migrate during their next major.',
    tags: ['postgres', 'mysql', 'architecture'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'database', kind: 'pattern',
    title: 'Always use uuidv7 for new tables',
    body: 'For new tables, use uuidv7 as the primary key. Time-sortable, no global lock, decent index locality. Drizzle helper in libs/db/uuid.ts.',
    tags: ['uuid', 'postgres', 'schema'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'database', kind: 'anti_pattern',
    title: 'Do not use SELECT * in service code',
    body: 'SELECT * is fragile under schema additions and pulls unused columns into memory. List columns explicitly. The only allowed exception is ad-hoc psql queries.',
    tags: ['sql', 'select-star', 'anti-pattern'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'rate-limiting', kind: 'pattern',
    title: 'Token bucket via Redis with EVALSHA',
    body: 'Rate limiting uses a single Redis instance with a Lua script. We pre-load the script with EVALSHA on startup. Bucket size is per-tier; tiers in libs/billing/tiers.ts.',
    tags: ['redis', 'rate-limit', 'lua'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'observability', kind: 'pattern',
    title: 'OpenTelemetry traces for all outbound HTTP',
    body: 'Wrap fetch with our otel helper in libs/otel/fetch.ts. Required for outbound calls; optional for internal RPC. Trace IDs flow through user-id and order-id custom attributes.',
    tags: ['otel', 'tracing', 'http'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'observability', kind: 'gotcha',
    title: 'Datadog logs lose lines >256KB',
    body: 'Lines over 256KB are silently truncated in Datadog. Stack traces from large JSON payloads get cut. We chunk via libs/log/chunk.ts when payload exceeds 200KB.',
    tags: ['datadog', 'logging'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'deploy', kind: 'deployment',
    title: 'Blue/green via Kubernetes service swap',
    body: 'Deploys do not use rolling. We deploy v2 alongside v1, run smoke tests against v2, then swap the service selector. Rollback is just swapping back. See infra/k8s/blue-green.md.',
    tags: ['kubernetes', 'deploy', 'blue-green'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'deploy', kind: 'gotcha',
    title: 'Argo Rollouts pause does not abort PreSync hook',
    body: 'Argo Rollouts pause stops new replica creation but does not stop in-flight PreSync hooks. A long PreSync continues to completion even after manual abort. Wrap PreSync in a 5min timeout.',
    tags: ['argo', 'kubernetes', 'gotcha'] },

  // ── github.com/acme/web ────────────────────────────────────────────
  { id: next(), repo: 'github.com/acme/web', topic: 'state', kind: 'decision',
    title: 'Zustand for client state, TanStack Query for server state',
    body: 'Decided to drop Redux. Zustand for ephemeral UI state, TanStack Query for cached server data. Two libs, two responsibilities. Migrating modules during normal feature work.',
    tags: ['zustand', 'react-query', 'state'] },
  { id: next(), repo: 'github.com/acme/web', topic: 'state', kind: 'gotcha',
    title: 'Zustand selector returning new object every render = infinite loop',
    body: 'A selector that returns a new object literal each call (e.g. `(s) => ({ a: s.a, b: s.b })`) without shallow eq triggers infinite re-render. Use `shallow` from zustand/shallow.',
    tags: ['zustand', 'react'] },
  { id: next(), repo: 'github.com/acme/web', topic: 'forms', kind: 'pattern',
    title: 'react-hook-form + zod resolver',
    body: 'All forms use react-hook-form with @hookform/resolvers/zod. Validation schemas live next to the form component. Server-side handlers re-validate with the same schema.',
    tags: ['react-hook-form', 'zod', 'forms'] },
  { id: next(), repo: 'github.com/acme/web', topic: 'styling', kind: 'convention',
    title: 'Tailwind with cva for variants',
    body: 'Component variants use class-variance-authority (cva). Avoid runtime cn(...) chains for variant logic. Pure Tailwind for one-off styles.',
    tags: ['tailwind', 'cva', 'styling'] },
  { id: next(), repo: 'github.com/acme/web', topic: 'a11y', kind: 'gotcha',
    title: 'Radix Dialog and Tooltip clash on Esc',
    body: 'When a Radix Tooltip and Dialog are both open, Esc closes both because both register identical handlers. Wrap Tooltip with a Dialog.Portal to scope the listener.',
    tags: ['radix', 'a11y', 'react'] },
  { id: next(), repo: 'github.com/acme/web', topic: 'build', kind: 'deployment',
    title: 'Vite build with manual chunks for monaco',
    body: 'Monaco Editor + xterm.js inflate the bundle past 4MB. We split them into manual chunks via vite.config.ts and lazy-load on the editor page.',
    tags: ['vite', 'monaco', 'bundle'] },

  // ── github.com/acme/infra ──────────────────────────────────────────
  { id: next(), repo: 'github.com/acme/infra', topic: 'terraform', kind: 'convention',
    title: 'One module per AWS account/region pair',
    body: 'Each AWS account+region gets its own Terraform module. State lives in S3 with DynamoDB locks. Avoid cross-region modules — they obscure dependencies.',
    tags: ['terraform', 'aws'] },
  { id: next(), repo: 'github.com/acme/infra', topic: 'terraform', kind: 'gotcha',
    title: 'Terraform import does not handle aws_iam_policy attachments well',
    body: 'aws_iam_policy_attachment is non-idempotent. Use aws_iam_role_policy_attachment instead. Import script in scripts/tf-import-policies.sh handles the migration.',
    tags: ['terraform', 'iam', 'aws'] },
  { id: next(), repo: 'github.com/acme/infra', topic: 'github-actions', kind: 'pattern',
    title: 'Reusable workflows for common CI shapes',
    body: 'Common shapes (test, build, deploy) live as reusable workflows under .github/workflows/_*.yml. Concrete workflows just call them. Reduces config drift.',
    tags: ['github-actions', 'ci'] },
  { id: next(), repo: 'github.com/acme/infra', topic: 'github-actions', kind: 'gotcha',
    title: 'OIDC token refresh stalls past 6h job',
    body: "GitHub's OIDC token expires at 6h. Long-running jobs that aws-sts:assume-role at the start need to re-issue or use the new sts hop pattern. See pull/2241.",
    tags: ['github-actions', 'oidc', 'aws'] },
  { id: next(), repo: 'github.com/acme/infra', topic: 'secrets', kind: 'pattern',
    title: 'Vault-injected env via init container',
    body: 'Production pods get secrets via the Vault init container, not at build time. Static secrets in Helm values are forbidden. The vault-injector annotation lives in libs/charts/_secrets.tpl.',
    tags: ['vault', 'kubernetes', 'secrets'] },
  { id: next(), repo: 'github.com/acme/infra', topic: 'logging', kind: 'integration',
    title: 'Vector → Loki with retention 14d/45d/180d',
    body: 'We ship logs through Vector to Loki with retention tiers: 14d for debug, 45d for info, 180d for audit. Cost-driven. Class set via the log_class log field.',
    tags: ['vector', 'loki', 'logging'] },

  // ── github.com/lab/research ────────────────────────────────────────
  { id: next(), repo: 'github.com/lab/research', topic: 'ml', kind: 'decision',
    title: 'PyTorch over JAX for production',
    body: 'Production training stays on PyTorch — better Triton + custom CUDA story for our deployment shape. JAX kept for research notebooks but not new prod jobs.',
    tags: ['pytorch', 'jax', 'ml'] },
  { id: next(), repo: 'github.com/lab/research', topic: 'ml', kind: 'gotcha',
    title: 'mixed-precision overflow on positional embeddings',
    body: 'fp16 mixed precision overflows silently on long-context positional embeddings. Cast positional encodings to fp32 then back. See utils/fp16.py.',
    tags: ['pytorch', 'fp16', 'numerical'] },
  { id: next(), repo: 'github.com/lab/research', topic: 'data', kind: 'pattern',
    title: 'Parquet partitioning by date and source',
    body: 'All ingested datasets land as Parquet partitioned by ingest_date and source. Schemas evolve via Iceberg-style metadata.',
    tags: ['parquet', 'iceberg', 'data'] },
  { id: next(), repo: 'github.com/lab/research', topic: 'training', kind: 'dead_end',
    title: 'Tried tensor parallel with FSDP — abandoned for 70B models',
    body: 'Combining tensor parallel + FSDP at >70B ran into deadlocks during checkpoint resharding. Switched to ZeRO-3 + pipeline parallel. PRs/2199 has the timeline.',
    tags: ['fsdp', 'tensor-parallel', 'training'] },
  { id: next(), repo: 'github.com/lab/research', topic: 'eval', kind: 'pattern',
    title: 'Eval harness uses OpenCompass + custom scorers',
    body: 'We use OpenCompass as the runner with custom scorers under evals/. New benchmarks register via evals/registry.py. CI runs the smoke set; nightly runs the full set.',
    tags: ['opencompass', 'eval'] },

  // ── github.com/zoo/oss ────────────────────────────────────────────
  { id: next(), repo: 'github.com/zoo/oss', topic: 'releases', kind: 'pattern',
    title: 'Conventional commits + changesets for npm publish',
    body: 'OSS repo uses conventional commits and Changesets for version bumps. CI on main runs the changeset publish flow and tags releases.',
    tags: ['changesets', 'release', 'npm'] },
  { id: next(), repo: 'github.com/zoo/oss', topic: 'docs', kind: 'convention',
    title: 'README is the single source of truth',
    body: 'Other docs reference the README. No long-form docs that duplicate setup. README pages benchmark against the project length: terse and evidence-first.',
    tags: ['readme', 'docs'] },
  { id: next(), repo: 'github.com/zoo/oss', topic: 'docs', kind: 'gotcha',
    title: 'GitHub markdown does not render task-list checkbox in tables',
    body: '`- [ ]` checkboxes in tables render as literal text on github.com (rendered fine in editors). Use ✅/⬜ glyphs in tables instead.',
    tags: ['markdown', 'github'] },
  { id: next(), repo: 'github.com/zoo/oss', topic: 'tests', kind: 'convention',
    title: 'node:test, no Jest',
    body: 'OSS modules use node:test. Avoid Jest — slower start, larger transitive footprint. Mocks via node:module register.',
    tags: ['node-test', 'jest', 'tests'] },
  { id: next(), repo: 'github.com/zoo/oss', topic: 'lint', kind: 'convention',
    title: 'Biome over ESLint+Prettier',
    body: 'Lint and format via Biome. Single config in biome.json. PR template requires `npm run lint` to pass.',
    tags: ['biome', 'eslint', 'lint'] },

  // additional cross-cutting & misc to round to ~60
  { id: next(), repo: 'github.com/acme/api', topic: 'caching', kind: 'pattern',
    title: 'Cache-Control:s-maxage on edge for product detail',
    body: 'Product detail pages are heavily cached at the edge with `s-maxage=3600, stale-while-revalidate=86400`. Personalization injects via Edge KV, not origin response.',
    tags: ['cdn', 'cache', 'edge'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'graphql', kind: 'anti_pattern',
    title: 'Do not expose mutations that loop over user-supplied ids',
    body: 'Mutations that accept an array of ids and loop must page or limit. We had a $5k/day cost spike from a mutation that accepted unbounded id lists. Limit at 50.',
    tags: ['graphql', 'incident', 'limits'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'graphql', kind: 'pattern',
    title: 'Persisted queries for production traffic',
    body: 'Production GraphQL is persisted-query only. Hashes in apollo-engine. Dynamic queries allowed only on internal admin tools.',
    tags: ['graphql', 'persisted-queries', 'security'] },
  { id: next(), repo: 'github.com/acme/web', topic: 'images', kind: 'pattern',
    title: 'next/image with cloudflare loader',
    body: 'All images go through next/image with the Cloudflare images loader. Avoid raw <img>. The loader is configured in next.config.js.',
    tags: ['next', 'images', 'cloudflare'] },
  { id: next(), repo: 'github.com/acme/web', topic: 'i18n', kind: 'integration',
    title: 'Crowdin import via GH Action; locale fallback en-US',
    body: 'Translations sync from Crowdin via a daily Action. Default locale is en-US; missing keys fall back to en-US not the source string.',
    tags: ['i18n', 'crowdin'] },
  { id: next(), repo: 'github.com/acme/web', topic: 'monitoring', kind: 'integration',
    title: 'Sentry tracesSampleRate 0.05 in prod, 1.0 in canary',
    body: 'Production Sentry sampling is 0.05 to keep budget; canary deploys run at 1.0 for 24h. Sample rate is set via NEXT_PUBLIC_SENTRY_RATE.',
    tags: ['sentry', 'monitoring'] },
  { id: next(), repo: 'github.com/acme/infra', topic: 'cost', kind: 'gotcha',
    title: 'Aurora I/O optimized vs standard — switching costs $$',
    body: 'Switching an Aurora cluster between I/O-optimized and standard incurs a one-time data transfer charge (~$0.02/GB). Plan switches for off-peak.',
    tags: ['aurora', 'aws', 'cost'] },
  { id: next(), repo: 'github.com/acme/infra', topic: 'security', kind: 'pattern',
    title: 'AWS IAM Identity Center for human access',
    body: 'Humans authenticate via Identity Center; access keys forbidden for humans. CI uses OIDC. Long-lived keys are an audit finding.',
    tags: ['iam', 'sso', 'aws'] },
  { id: next(), repo: 'github.com/lab/research', topic: 'data', kind: 'gotcha',
    title: 'CSV from spreadsheets has BOM that pandas mis-parses',
    body: 'Excel-exported CSVs include a UTF-8 BOM (0xEF 0xBB 0xBF). pandas read_csv adds a column ﻿id. Use encoding="utf-8-sig".',
    tags: ['pandas', 'csv'] },
  { id: next(), repo: 'github.com/lab/research', topic: 'training', kind: 'pattern',
    title: 'Save checkpoints on epoch boundaries via callback',
    body: 'Use a TrainerCallback to checkpoint on epoch end and on signal. Avoid saving every N steps for small datasets — disk thrashing.',
    tags: ['training', 'checkpoint'] },
  { id: next(), repo: 'github.com/zoo/oss', topic: 'security', kind: 'pattern',
    title: 'Pin npm deps with lockfileVersion 3',
    body: 'OSS repos pin via package-lock.json with lockfileVersion 3. Renovate runs weekly. CI fails if lockfile is missing.',
    tags: ['npm', 'security', 'pin'] },
  { id: next(), repo: 'github.com/zoo/oss', topic: 'releases', kind: 'gotcha',
    title: 'Changesets ignores non-release commits in monorepos',
    body: 'In a Changesets monorepo, commits that touch only fixture or doc files do not bump a version. Add a `.changeset/empty.md` if a release is needed for CI gating.',
    tags: ['changesets', 'monorepo'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'security', kind: 'pattern',
    title: 'Helmet + strict CSP from src/middleware/security.ts',
    body: 'All HTTP servers attach the security middleware on boot. CSP is strict; nonce-based. Inline scripts forbidden outside the bootstrap.',
    tags: ['helmet', 'csp', 'security'] },
  { id: next(), repo: 'github.com/acme/api', topic: 'security', kind: 'anti_pattern',
    title: 'Do not ignore CORS preflight failures in tests',
    body: 'Tests sometimes mocked CORS preflight to 200; production then 403d. Tests must hit real CORS via integration suite. See test/integration/cors.test.ts.',
    tags: ['cors', 'tests'] },
  { id: next(), repo: 'github.com/acme/web', topic: 'router', kind: 'decision',
    title: 'TanStack Router for new app, Next.js for marketing',
    body: 'Marketing site stays on Next.js (SSG-heavy). The application UI uses TanStack Router for typed search params and parallel data fetching. Two routers, two scopes.',
    tags: ['tanstack-router', 'next', 'router'] },
  { id: next(), repo: 'github.com/acme/web', topic: 'forms', kind: 'gotcha',
    title: 'react-hook-form values do not reflect default after navigation',
    body: 'After router navigation re-uses a form component, defaultValues do not refire. Use useEffect + reset() on the route loader response, or pass `key={route.path}`.',
    tags: ['react-hook-form', 'router'] },
  { id: next(), repo: 'github.com/acme/web', topic: 'a11y', kind: 'pattern',
    title: 'aria-live="polite" for toast region',
    body: 'Toasts mount in a single aria-live="polite" region under <body>. SR users hear the toast text without focus shift. Implementation in libs/ui/Toast.tsx.',
    tags: ['a11y', 'toast'] },
  { id: next(), repo: 'github.com/acme/infra', topic: 'cost', kind: 'pattern',
    title: 'S3 lifecycle to Glacier IR after 90d',
    body: 'Audit logs and large datasets move to Glacier IR after 90d. Lifecycle in libs/tf/s3-lifecycle/main.tf. Saves ~$1.2k/mo in our heaviest bucket.',
    tags: ['s3', 'cost', 'aws'] },
  { id: next(), repo: 'github.com/lab/research', topic: 'eval', kind: 'gotcha',
    title: 'BLEU collapses on near-empty refs — use chrF for short outputs',
    body: 'BLEU scores collapse when reference text is shorter than the n-gram window. Use chrF for outputs <50 chars. See evals/scorers/short.py.',
    tags: ['bleu', 'chrf', 'eval'] },
  { id: next(), repo: 'github.com/zoo/oss', topic: 'mcp', kind: 'integration',
    title: 'MCP stdio servers must NOT log to stdout',
    body: 'Anything written to stdout from a stdio MCP server breaks the JSON-RPC framing. All logs to stderr. We saw this with a "console.log debugging" PR that broke clients.',
    tags: ['mcp', 'stdio', 'logging'] },
];

// ── 50 hand-labeled queries ─────────────────────────────────────────
//
// `relevant` lists the seed entry ids that should appear in top-5.
// The retriever is graded on precision@5 against this set.
//
// Tag conventions: queries are written as a developer might type them in
// chat — partial sentences, missing terms, occasional misspellings.

export const QUERIES = [
  // auth / jwt
  { repo: 'github.com/acme/api', q: 'how do we verify JWTs', relevant: ['seed-1'] },
  { repo: 'github.com/acme/api', q: 'auth0 jwks rotation cache', relevant: ['seed-2'] },
  { repo: 'github.com/acme/api', q: 'jwt library choice', relevant: ['seed-1'] },
  { repo: 'github.com/acme/api', q: '401 errors after auth0 update', relevant: ['seed-2'] },

  // billing
  { repo: 'github.com/acme/api', q: 'stripe webhook retries', relevant: ['seed-3', 'seed-4'] },
  { repo: 'github.com/acme/api', q: 'stripe idempotency keys', relevant: ['seed-3', 'seed-4'] },
  { repo: 'github.com/acme/api', q: 'avoid double charging', relevant: ['seed-4', 'seed-3'] },

  // database / postgres
  { repo: 'github.com/acme/api', q: 'why postgres', relevant: ['seed-5'] },
  { repo: 'github.com/acme/api', q: 'primary key strategy for new tables', relevant: ['seed-6'] },
  { repo: 'github.com/acme/api', q: 'select star is bad', relevant: ['seed-7'] },
  { repo: 'github.com/acme/api', q: 'uuid v7', relevant: ['seed-6'] },

  // rate limit
  { repo: 'github.com/acme/api', q: 'rate limiting strategy', relevant: ['seed-8'] },
  { repo: 'github.com/acme/api', q: 'redis lua script for limits', relevant: ['seed-8'] },

  // observability
  { repo: 'github.com/acme/api', q: 'tracing outbound http', relevant: ['seed-9'] },
  { repo: 'github.com/acme/api', q: 'datadog truncating logs', relevant: ['seed-10'] },
  { repo: 'github.com/acme/api', q: 'log line size limit', relevant: ['seed-10'] },

  // deploy
  { repo: 'github.com/acme/api', q: 'how do we deploy', relevant: ['seed-11'] },
  { repo: 'github.com/acme/api', q: 'argo rollouts pause not stopping presync', relevant: ['seed-12'] },
  { repo: 'github.com/acme/api', q: 'rollback strategy', relevant: ['seed-11'] },

  // web state
  { repo: 'github.com/acme/web', q: 'state management library', relevant: ['seed-13'] },
  { repo: 'github.com/acme/web', q: 'zustand re-rendering loop', relevant: ['seed-14'] },
  { repo: 'github.com/acme/web', q: 'redux replacement', relevant: ['seed-13'] },

  // forms / styling / a11y
  { repo: 'github.com/acme/web', q: 'form validation', relevant: ['seed-15'] },
  { repo: 'github.com/acme/web', q: 'tailwind variant pattern', relevant: ['seed-16'] },
  { repo: 'github.com/acme/web', q: 'radix tooltip dialog escape conflict', relevant: ['seed-17'] },
  { repo: 'github.com/acme/web', q: 'monaco bundle size', relevant: ['seed-18'] },

  // infra terraform
  { repo: 'github.com/acme/infra', q: 'terraform module layout', relevant: ['seed-19'] },
  { repo: 'github.com/acme/infra', q: 'iam policy attachment migration', relevant: ['seed-20'] },

  // gh actions
  { repo: 'github.com/acme/infra', q: 'reusable workflow conventions', relevant: ['seed-21'] },
  { repo: 'github.com/acme/infra', q: 'oidc token expiry on long jobs', relevant: ['seed-22'] },

  // secrets / logging
  { repo: 'github.com/acme/infra', q: 'how do prod secrets get into pods', relevant: ['seed-23'] },
  { repo: 'github.com/acme/infra', q: 'log retention policy', relevant: ['seed-24'] },

  // research / ml
  { repo: 'github.com/lab/research', q: 'pytorch vs jax', relevant: ['seed-25'] },
  { repo: 'github.com/lab/research', q: 'fp16 numerical issue positional', relevant: ['seed-26'] },
  { repo: 'github.com/lab/research', q: 'how do we partition parquet', relevant: ['seed-27'] },
  { repo: 'github.com/lab/research', q: 'why we abandoned fsdp tensor parallel', relevant: ['seed-28'] },
  { repo: 'github.com/lab/research', q: 'eval harness', relevant: ['seed-29'] },
  { repo: 'github.com/lab/research', q: 'csv bom problem', relevant: ['seed-44'] },
  { repo: 'github.com/lab/research', q: 'when to checkpoint training', relevant: ['seed-45'] },
  { repo: 'github.com/lab/research', q: 'short answer eval scoring', relevant: ['seed-50'] },

  // oss
  { repo: 'github.com/zoo/oss', q: 'release process', relevant: ['seed-30'] },
  { repo: 'github.com/zoo/oss', q: 'doc convention', relevant: ['seed-31'] },
  { repo: 'github.com/zoo/oss', q: 'task list rendering broken', relevant: ['seed-32'] },
  { repo: 'github.com/zoo/oss', q: 'test runner choice', relevant: ['seed-33'] },
  { repo: 'github.com/zoo/oss', q: 'lint formatter setup', relevant: ['seed-34'] },
  { repo: 'github.com/zoo/oss', q: 'mcp server logging gotcha', relevant: ['seed-51'] },

  // cross-cutting
  { repo: 'github.com/acme/api', q: 'cdn caching strategy', relevant: ['seed-35'] },
  { repo: 'github.com/acme/api', q: 'graphql cost spike', relevant: ['seed-36'] },
  { repo: 'github.com/acme/api', q: 'persisted queries', relevant: ['seed-37'] },
];
